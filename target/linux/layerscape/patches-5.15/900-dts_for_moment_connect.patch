--- a/arch/arm64/boot/dts/freescale/Makefile
+++ b/arch/arm64/boot/dts/freescale/Makefile
@@ -34,6 +34,8 @@ dtb-$(CONFIG_ARCH_LAYERSCAPE) += fsl-lx2
 dtb-$(CONFIG_ARCH_LAYERSCAPE) += traverse-ls1043v.dtb
 dtb-$(CONFIG_ARCH_LAYERSCAPE) += traverse-ls1043s.dtb
 
+dtb-$(CONFIG_ARCH_LAYERSCAPE) += moment-ls1088a-connect.dtb
+
 dtb-$(CONFIG_ARCH_MXC) += imx8mm-beacon-kit.dtb
 dtb-$(CONFIG_ARCH_MXC) += imx8mm-evk.dtb
 dtb-$(CONFIG_ARCH_MXC) += imx8mm-ddr4-evk.dtb
--- a/arch/arm64/boot/dts/freescale/moment-ls1088a-connect.dts
+++ b/arch/arm64/boot/dts/freescale/moment-ls1088a-connect.dts
@@ -1,3 +1,13 @@
+// SPDX-License-Identifier: (GPL-2.0+ OR MIT)
+/*          
+ * Device Tree file for Moment LS1088A Connect Board.
+ *  
+ * Copyright 2022-2023 Moment
+ *  
+ * Joris Roussel <joris.roussel@quetzal-lab.com>
+ *  
+ */
+
 /dts-v1/;
 
 / {
@@ -5,7 +15,7 @@
 	interrupt-parent = <0x01>;
 	#address-cells = <0x02>;
 	#size-cells = <0x02>;
-	model = "TQ TQMLS1088A SoM on Moment board";
+	model = "TQ TQMLS1088A SoM on Moment Connect board";
 
 	aliases {
 		crypto = "/soc/crypto@8000000";
